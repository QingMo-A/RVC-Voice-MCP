# Local RVC models

Place one RVC `.pth` file and its matching `.index` file here. Subdirectories are supported.

When exactly one file of each type exists, the server selects them automatically. Configure `RVC_MODEL_PATH` and `RVC_INDEX_PATH` in `.env` when storing multiple voices.

Everything in this directory except this file is ignored by Git.
